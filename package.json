{
  "name": "discovery-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/stamp-sw.mjs dist",
    "preview": "vite preview --host 127.0.0.1",
    "build:private": "vite build --mode private && cp docs/sw.js dist-private/sw.js && cp -R private-api/. dist-private/ && node scripts/stamp-sw.mjs dist-private",
    "test": "python3 scripts/test_build_discovery_geojson.py && python3 scripts/test_build_places_index.py"
  },
  "dependencies": {
    "@mapbox/maki": "^8.2.0",
    "@vercel/blob": "^2.8.0",
    "maplibre-gl": "^5.9.0"
  },
  "devDependencies": {
    "vite": "^7.1.12"
  }
}
