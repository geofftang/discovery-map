{
  "name": "discovery-map-private-deploy",
  "private": true,
  "type": "module",
  "description": "Deployed root of the OWNER build: static bundle + api/edits.js. Copied into dist-private/ by `npm run build:private`. Never deployed publicly.",
  "dependencies": {
    "@vercel/blob": "^2.8.0"
  }
}
